#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
  uint32_t n = 0, m = 0;
  cin >> n >> m;

  vector<uint32_t> a[n], b[n];
  for (int i = 0; i < n; ++i) {
    uint32_t A, B;
    cin >> A >> B;
    a[i] = A;
    b[i] = B;
  }

  for (int j = 0; j < m; ++j) {

  }
  return 0;
}